1)
Paziente rossi[]
2)
Pazienti clinica[20]
3)
for(i=0; i<n; i++){
	printf("Inserisci il nome del paziente:");
	scanf("%s", a[i].nome);
	printf("Inserisci il cognome del paziente:");
	scanf("%s", a[i].cognome);
	printf("Inserisci la matricola:");
	scanf("%d", &a[i].matricola);
    printf("Inserisci la pressione:");
	scanf("%d", &a[i].pressione);
    printf("Inserisci la temperatura:");
	scanf("%d", &a[i].temperatura);
    printf("Inserisci la parcella:");
	scanf("%d", &a[i].parcella);
}
4)
printf("Pressione %d", a.pressione)
printf("Temperatura %d", a.temperatura)
